query Entry ($eid: Int!) {
  entry(eid: $eid) {
    children {
      content
      eid
      expanded
      name
      parentEid
      rendered
    }
    content
    eid
    expanded
    name
    parentEid
    rendered
  }
}
